# Fonte de verdade das dependências está em pyproject.toml.
# Este arquivo é mantido para compatibilidade com fluxos legados de instalação.
# Para instalar o pacote:
#     pip install .            (ou)
#     pip install -e ".[dev]"
-e .
